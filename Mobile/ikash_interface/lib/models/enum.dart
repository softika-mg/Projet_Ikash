enum OperatorType { telma, airtel, orange, autre }

enum TransactionType { depot, retrait, transfert, credit }

enum TransactionStatus { reussi, echec, enAttente, archivee }

enum RoleType { admin, agent }
